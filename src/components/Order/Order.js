import React from 'react';
import classes from './Order.css';

const order = (props) => (
	<div className={classes.Order}>
		<p>Ingredients: {this.props.ingredients}</p>
		<p>Price: <strong>{this.props.ingredients}</strong></p>
	</div>
);

export default order;
